/* AutoHaus vehicle inventory is managed live through Supabase.
   Deliberately empty: a stale bundled catalogue must never resurrect sold cars
   or hide newly published listings when the managed source changes.
   Build marker: monochrome-admin-v1. */
window.AH_VEHICLES = [];
